{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "bunVersion": "1.3.2",
    "buildCommand": "bun run build",
    "outputDirectory": "api",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ]
}