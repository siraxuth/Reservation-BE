{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "bunVersion": "1.3.2",
    "buildCommand": "bunx prisma generate && bun run build",
    "outputDirectory": "api",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ]
}